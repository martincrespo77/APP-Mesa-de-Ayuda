meta {
  name: 12 Obtener Requerimiento Sin Token
  type: http
  seq: 12
}

get {
  url: {{base_url}}/requerimientos/{{incidente_id}}
  auth: none
}

assert {
  res.status: eq 401
}

docs {
  Fix de seguridad: antes GET /requerimientos/{id} no exigía autenticación.
}
